# Python packages for the bulk-validation workflow
# Recommended Python version: 3.10 or newer
#
# Install with:
#   python -m pip install -r env_bulk_validation.txt
#
# GCC is also required to compile the C programs, but it is not a Python package.

numpy>=1.24
matplotlib>=3.7
imageio-ffmpeg>=0.4.9
jupyterlab>=4.0
ipykernel>=6.0
